<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mainactivity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MainActivity">
        <activity android:name=".OnlinePayment"></activity>
        <activity android:name=".PaymentMethod" />
        <activity android:name=".PlaceOrder" />
        <activity android:name=".DessertAndBeverages" />
        <activity android:name=".PartyPackage" />
        <activity android:name=".HappyMealMenu" />
        <activity android:name=".SecretMenu" />
        <activity android:name=".RegularMenu" />
        <activity android:name=".FoodTray" />
        <activity android:name=".McdoMenu" />
        <activity android:name=".BreakfastMenu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>